<%= link_to gravatar_for(current_user, size: Settings.micropost.user), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("micropost.view_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.size, t("micropost.micr")) %></span>
